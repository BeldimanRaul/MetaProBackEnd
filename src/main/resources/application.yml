spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/metapro
    username: postgres
    password: caragiale17


  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: emailul.tau@gmail.com
    password: parola_aplica?iei
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    user:
      name: admin
      password: admin
    basic:
      enabled: false
